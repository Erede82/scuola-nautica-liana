import 'package:flutter/material.dart';

/// Chiave del [Navigator] root ([MaterialApp]) — usata per ripulire lo stack dopo logout.
final GlobalKey<NavigatorState> appRootNavigatorKey = GlobalKey<NavigatorState>();
